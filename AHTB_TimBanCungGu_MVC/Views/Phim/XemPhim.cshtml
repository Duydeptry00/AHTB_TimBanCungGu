@{
    ViewData["Title"] = "Xem Phim";
    var phan = ViewBag.Phan as AHTB_TimBanCungGu_API.Models.Phan;
    var phimDeCu = ViewBag.PhimDeCu as List<AHTB_TimBanCungGu_API.Models.Phim>;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">


    <style>

        .film-player {
            width: 100%;
            height: 500px;
            background-size: cover;
            background-position: center;
            border-radius: .375rem;
            overflow: hidden;
            margin-bottom: 20px;
        }

        .episode-buttons {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 20px;
        }

            .episode-buttons button {
                background-color: #383d41;
                border: none;
                cursor: pointer;
                color: #fff;
                font-size: 15px;
                transition: background-color 0.3s ease, transform 0.3s ease;
                width: 60px;
                border-radius: .375rem; /* Add rounded corners */
                padding: 5px; /* Add some padding */
            }

                .episode-buttons button:hover {
                    background-color: #585c61;
                    transform: scale(1.05);
                }

        .card-hover {
            position: relative;
            overflow: hidden;
            transition: transform 0.3s ease-in-out;
            background-color: #343a40;
            color: #f8f9fa;
        }

            .card-hover img {
                width: 100%;
                height: 350px;
                object-fit: cover;
                transition: filter 0.3s ease-in-out;
                border-radius: .375rem; /* Consistent border radius */
            }

            .card-hover:hover img {
                filter: brightness(80%);
            }

            .card-hover:hover {
                transform: scale(1.05);
            }

        .card-body {
            padding: 15px;
        }

        .btn-xem-ngay {
            position: absolute;
            opacity: 0;
            pointer-events: none;
            display: flex;
            align-items: center;
            justify-content: center;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: #dc3545;
            border: none;
            color: white;
            padding: 12px 24px;
            border-radius: 30px; /* Điều chỉnh để có góc bo tròn hơn */
            font-size: 1rem; /* Để dễ đọc hơn */
            min-width: 150px;
            text-align: center;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.25); /* Tăng độ bóng để tạo chiều sâu */
            transition: opacity 0.3s ease, transform 0.3s ease, background-color 0.3s ease;
            cursor: pointer;
            font-weight: bold; /* Chữ đậm để dễ nhìn hơn */
        }

        .card-hover:hover .btn-xem-ngay {
            opacity: 1;
            pointer-events: auto; /* Cho phép tương tác khi hover */
            background-color: #c82333; /* Màu đỏ đậm hơn khi hover */
            transform: translate(-50%, -50%) scale(1.05); /* Tăng kích thước một chút khi hover */
        }

        .btn-xem-ngay i {
            margin-right: 8px;
            font-size: 18px; /* Tăng kích thước biểu tượng cho cân đối hơn */
        }
        .card-title {
            font-size: 16px;
            color: #f8f9fa;
        }

        .pagination {
            margin-top: 20px;
        }

     /*    .film-card {
            border: none;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

            .film-card img {
                border-radius: .375rem;
                max-height: 300px;
                object-fit: cover;
            }

            .film-card:hover {
                transform: scale(1.05);
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            }

            .film-card .card-body {
                background-color: #f8f9fa;
                padding: 1rem;
            }

            .film-card .card-title {
                font-size: 1rem;
                color: #333;
                margin-top: .5rem;
            } */

   </style>
</head>
<body>
    <div class="container my-4">
        <section class="mb-4">
            <h1>
                @phan.Phim.TenPhim
                @if (phan.SoPhan != null)
                {
                    @($"(Phần {phan.SoPhan})")
                }
            </h1>

            <div class="film-player bg-image">
                <iframe id="filmPlayer" src="https://vidsrc.me/embed/tv?imdb=@phan.Phim.SourcePhim&season=@phan.SoPhan&episode=0" style="width: 100%; height: 100%;" frameborder="0" referrerpolicy="origin" allowfullscreen></iframe>
            </div>

            <div class="episode-buttons">
                @if (phan.SoLuongTap != null)
                {
                    @for (int i = 1; i <= phan.SoLuongTap; i++)
                    {
                        <button onclick="doiTap(@i)"> @i</button>
                    }
                }
            </div>

            <div class="mb-4">
                <h2>Mô tả</h2>
                <p>@phan.Phim.MoTa</p>
            </div>
        </section>

        <script>
            function doiTap(soTap) {
                const sourcePhim = "@phan.Phim.SourcePhim";
                const soPhan = @phan.SoPhan;
                const iframe = document.getElementById('filmPlayer');
                iframe.src = `https://vidsrc.me/embed/tv?imdb=${sourcePhim}&season=${soPhan}&episode=${soTap}`;
            }
        </script>

        <section class="mb-4">
            <h2>Phim đề cử</h2>
            <div class="row">
                @foreach (var phim in phimDeCu)
                {
                    <div class="col-md-3 mb-4">
                        <div class="card-hover">
                            <img src="/images/@phim.HinhAnh" alt="@phim.TenPhim" class="card-img-top">
                            <div class="card-body">
                                <h5 class="card-title">@phim.TenPhim</h5>
                                <button class="btn-xem-ngay">
                                    <a asp-controller="Phim" asp-action="ChiTietPhim" asp-route-id="@phim.IDPhim" style="text-decoration: none; color: white;">
                                        <i class="bi bi-play-fill"></i> Xem ngay
                                    </a>
                                </button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </section>
    </div>

    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>